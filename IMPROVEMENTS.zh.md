# 相对原版基座改了什么

DeepSeek Harness 官方发布的是命令行插件内核。本构建是把它变成 Windows 桌面产品的个人分支，并且是压在基座之上维护、而不是另起一条代码线。本页按"读者能做什么不同的事"分组列出新增与改动，并给出可以重算每个数字的命令。

## 目录

- [怎么读这些数字](#怎么读这些数字)
- [桌面外壳与打包](#桌面外壳与打包)
- [多代理团队](#多代理团队)
- [兄弟会话协同](#兄弟会话协同)
- [电脑与浏览器操作](#电脑与浏览器操作)
- [记忆与决策记录](#记忆与决策记录)
- [产品界面与中文本地化](#产品界面与中文本地化)
- [媒体与长视频](#媒体与长视频)
- [换底座的机制](#换底座的机制)
- [已知缺口](#已知缺口)

## 怎么读这些数字

2026-09-24 与同版本纯净基座副本逐文件比对：11,239 个基座文件变成 11,861 个，其中 440 个是改过的基座文件、622 个是这里新增的，没有删除任何文件。全部工作归入 40 个能力面，每个面各自声明合并策略与验证步骤。

在本分支的源码工作树里可以重算：

```powershell
python product-layer\tools\product-layer-verify.py
python -c "import json;m=json.load(open('product-layer/capsule/manifest.json',encoding='utf-8-sig'));print(m['counts'],m['blobFiles'],len(m['surfaces']))"
```

## 桌面外壳与打包

113 条。基座没有窗口，这个构建有，而且归它自己管。

- 自带红绿灯式窗口装饰的原生窗口，并在框架挂载之前先把启动期表面画出来，控件不会悬在一片空白上。
- 按用户安装的 Inno Setup 安装包，不需要管理员权限；另有可搬运的包布局，其依赖链接在首次启动时重建，而不是指向产出它的那台机器。
- 会在通知里点名"完成的是哪个任务"的 Windows 通知身份；提示音由渲染侧播放，因为打包后的应用无法按名字选系统音效。
- 图标与四段 Windows 版本资源被嵌进出厂的可执行文件。

## 多代理团队

163 条。基座能跑子代理，这个构建把它们当成一支你能看着干活的队伍来跑。

- 内置团队模板、办公室视图，以及成员的工作室设置。
- 按阶段卡的计划预算门，每个阶段都记有 owner。
- 界面上统一的 Agent 预设菜单，以及能看见每个成员在做什么的子代理面板。

## 兄弟会话协同

18 条。两个独立会话是协商，而不是互相覆盖文件。

- 一块持久看板加 `peer_*` 工具，兄弟会话可以发帖、租约、回帖。
- 这些工具只在存在活着的兄弟会话时挂载，单独一个会话不会看见自己用不了的控件。

## 电脑与浏览器操作

118 条。基座能触网，这个构建能触屏幕。

- 常驻桌面驱动负责指针与屏幕访问，在测试机上实测指针读取 2 ms、截屏 316 ms。
- 代理控制指示器，告诉你此刻是哪个成员在动这台机器，状态不需要图例就能读懂。
- 带 provider 深度能力的内置浏览器面板，以及为它适配过的网页搜索与抓取工具。

## 记忆与决策记录

178 条，其中 162 条是决策记录。

- 记忆的蒸馏、检索与固化。
- Agent Notes 是仓库的一等公民：每个非平凡改动都带着为什么这么做、放弃了什么、代价是什么。

## 产品界面与中文本地化

299 条。整个界面是重新组织并本地化的，不是就地翻译。

- 重排分区的设置壳、可用模型获取、插件与技能管理、带永久删除语义的归档页，以及把 16 个 `computer_*` 工具策略收在一处的操作页。
- 主题、背景图、桌宠，以及对首屏做的品牌与 Hero 处理。
- 技能与 MCP 市场。在出厂构建上实测能读到内置 12 条与 GitHub 20 条技能、MCP 注册表 165 条；技能安装落在 `dsh-home\skills`，MCP 安装写进 `cordis.patch.yml` 的受管区块。
- 安装后无需重启即生效：桌面 profile 会监听自己的用户 patch 层，下一轮对话就能调用新服务。面板只声明它的检查能证明的事——那一行确实在配置文件里。

## 媒体与长视频

18 条。媒体工具，以及按形态（画面、解说、剪辑节奏）而不是按写死的片段时长来规划的长视频团队。

## 换底座的机制

147 条。这一块保证上面所有部分不会在下次基座发布时被冲掉。

- 自包含胶囊：每个改过的基座文件都与它的纯净底座原文一起存着，原文按内容寻址。于是升级只需要新的官方源码加胶囊——不需要上一版干净官方树，也不需要手工合并。
- 三道登记账，各回答一个问题：工作树与胶囊是否一致、人读台账是否列全了产品路径、登记过的路径是否还存在。
- 一个升级驱动：胶囊与台账不一致时它拒绝复制任何文件，并能在同一次运行里把胶囊重新基准到新版本上。

## 已知缺口

- 两个已声明的能力面 `ptc-runtime` 与 `typert-generator` 当前持有 0 条：分类写好了，定制还没做。
- 本页数字会随着分支新增决策记录而变动，因为那些文件本身也属于产品层。请重跑上面的命令，不要相信被抄下来的数。
- 市场只报告它自己能证明的事。它还没有提供逐服务的"重连"与日志入口，而这正是"配置没错但上游连不上"时界面上查不下去的地方。

## Dev Note

本页每个数字都来自 2026-09-24 一次重捕后对 `product-layer/capsule/manifest.json` 的读取，或来自在出厂构建的运行实例里量到的结果。桌面驱动的耗时是单台 x64 机器的数，换机器会不同。市场的行为是通过在出厂界面里真装了一个技能和一个 MCP 服务验证的，不是只读代码得出的。
